# Kageos 产品计划

## 项目方向

Kageos 后续肯定是要开源的，同时主要面向海外市场。

第一批目标用户不是大企业，而是：

- 海外个人用户
- 独立开发者
- 自由职业者
- 中小企业
- 需要快速搭建内部工具的小团队

产品方向要围绕“能快速上手、能马上用起来、不要复杂企业级流程”来设计。

## 开源策略

核心项目会开源，并且免费使用。

具体使用什么开源协议目前还没有确定，需要后续单独讨论。

后续需要明确：

- 最终采用什么开源协议
- 哪些能力属于开源免费版
- 哪些能力属于托管收费版
- 社区贡献和商业版本之间的边界

## 托管服务

用户自己部署开源版本是免费的。

我们提供的托管云服务是收费的。托管版本主要解决个人用户和中小企业不想自己部署、不想维护服务器的问题。

托管服务可以提供的价值包括：

- 免部署
- 自动更新
- 托管运行环境
- 托管存储
- 团队权限管理
- 数据备份
- 更稳定的线上访问体验

## 模板策略

模板是很重要的一块，也是早期出海的核心卖点之一。

我们需要先提供一批高质量模板，让用户可以一键导入，然后直接改一改就能用。

第一批模板可以围绕个人和中小企业的高频场景来做，例如：

- 客户管理
- 库存管理
- 收银 / 小型零售
- 订单管理
- 预约管理
- 工单 / 任务管理
- 简单财务 / 报销 / 支出记录

模板不能只是演示数据，要尽量做成真实可用的业务工具。用户导入后应该能立刻感受到产品价值。

## Hub 策略

Hub 是官方闭源模板分发服务，用来承载高质量模板的展示、购买和交付。

第一版 Hub 先保持轻量：

- 模板包继续使用现有 `capability-bundle.json` 格式。
- Hub 负责展示商品、校验下载资格、交付 bundle JSON。
- 主项目拿到 bundle 后继续复用现有导入接口安装，不把订单、支付、上架逻辑塞进开源核心。
- 商品预览依赖 bundle 里的 `tree_nodes`、`docs` 等结构化元数据，方便展示模板目录、函数和文档。
- 下载先采用一次性 ticket，后续接入支付、订单和 license 后仍然可以沿用这条交付链路。

Hub 第一阶段不优先做复杂 DRM 或加密包。模板源码本身会在用户购买后交付给用户，重点先放在高质量模板、清晰预览、稳定安装和后续升级路径上。

## 盈利方式

目前先按下面这个方向考虑：

- 开源核心：免费
- 托管云服务：收费
- 高质量模板：收费

具体定价还没有确定。

后续可以考虑：

- 免费版：适合个人试用和轻量使用
- Pro 版：适合个人深度使用
- Team 版：适合小团队和中小企业
- 官方模板包：按模板或模板包收费
- 未来如果有模板市场，可以考虑抽成模式

## 当前优先级

第一阶段先把下面这些事情做好：

1. 英文体验打磨完整
2. 新用户 onboarding 简单清晰
3. 做出第一批高质量可导入模板
4. 托管云服务路径跑通
5. 基础收费路径跑通
6. 权限和操作日志做扎实，保证用户操作可追溯

## 轻量企业租户边界

当前阶段不做完整 Organization / Workspace 多租户大改，也不改 `full-code-path`。

短期采用更轻的企业归属模型：

- 用户注册时必须创建企业或加入已有企业。
- 企业 `code` 和企业名称先全局唯一，避免托管 SaaS 下出现搜索和展示歧义。
- 企业支持维护 Logo 图标地址，方便后续托管版展示企业品牌。
- 用户注册后绑定一个企业，第一版不支持用户切换企业。
- 用户登录后的 Token 必须能解析出企业信息，至少包含 `company_code`，并尽量携带企业名称和 Logo，方便网关、审计和下游服务直接拿到租户上下文。
- 用户搜索、授权候选、操作日志用户筛选等人员选择能力，只能看到同一企业下的用户。
- `full-code-path` 继续保持 `/{tenant_user}/{app}/{router}`，企业只作为用户搜索和后续租户隔离的结构化字段。
- 私有化部署通常只有一个企业，这套模型也能自然工作。

后续如果托管 SaaS 占比提高，再考虑邀请审批、多企业成员、Workspace 切换、计费组织等完整能力。

## 备注

这个文档先作为开发阶段的方向记录，后面可以继续补充开源协议、模板规划、定价、上线节奏和推广计划。
