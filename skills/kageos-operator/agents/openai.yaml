interface:
  display_name: "KageOS Operator"
  short_description: "Verify KageOS directories and complex inputs over HTTP"
  default_prompt: "Use $kageos-operator to verify this KageOS directory through direct authenticated HTTP calls, including files, users, rich text, business readback, and safe cleanup."
