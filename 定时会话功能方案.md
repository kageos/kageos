# 定时会话功能方案

## 1. 背景

当前系统已经有定时任务能力，适合到点执行确定函数，例如报表函数、表数据创建、表数据更新、消息通知等。这类任务的特点是输入输出固定、成本低、稳定可控。

但工作台还缺少另一类自动化能力：到点唤起 Agent，让它基于项目上下文进行检查、分析、诊断、总结或生成待确认操作建议。这类任务不是简单函数调用，执行路径会随上下文变化，因此更适合用“定时会话”承载。

本方案建议新增一套独立的 Agent 自动任务能力，不复用现有 `scheduled_task` 表，不把 Agent 会话硬塞进现有定时任务的 `action` 字段。

## 2. 产品定位

功能名称建议：

- 后端能力名：Scheduled Agent Task
- 产品入口名：Agent 自动任务
- 用户可见名称：定时会话 / 智能巡检 / Agent 自动执行

核心定位：

> 到达指定时间后，系统自动创建一次工作台 Agent 会话，让 Agent 按预设目标完成分析、巡检、诊断或生成建议。

它不是替代现有定时任务，而是补齐“目标明确但路径不固定”的自动化场景。

## 3. 和现有定时任务的区别

| 能力 | 现有定时任务 | 新增定时会话 |
| --- | --- | --- |
| 执行对象 | 函数、表操作、确定接口 | 工作台 Agent 会话 |
| 执行路径 | 固定 | 由 Agent 根据上下文选择 |
| 成本 | 通常不消耗 token | 会消耗 token |
| 稳定性 | 高 | 中等，依赖模型和工具 |
| 结果形态 | 函数响应、表数据、通知 | 会话、总结、工具调用记录、建议 |
| 适用场景 | 固定报表、数据同步、定时通知 | 项目巡检、异常分析、风险总结、自动诊断 |
| 权限控制 | 函数级权限 | 工具白名单、预算、审批、超时 |
| 风险 | 较低 | 需要治理 |

产品上建议统一放在“自动化”入口下，但分成两个类型：

- 函数任务：稳定、确定、低成本。
- Agent 任务：智能、灵活、会消耗 token。

## 4. 适用场景

### 4.1 项目巡检

每天或每周自动检查项目状态，例如：

- 最近有没有异常数据
- 表单是否有积压
- 图表是否渲染异常
- 权限配置是否存在明显问题
- 最近构建或发布是否失败

### 4.2 异常分析

函数任务可以先发现异常，定时会话负责进一步分析：

- 哪个部门、产品、客户或负责人导致异常
- 异常是否和历史变化有关
- 应该通知谁
- 是否需要生成待办

### 4.3 周报和风险总结

定期生成项目周报、运营周报、交付风险总结：

- 本周期完成了什么
- 哪些事项停滞
- 哪些数据有异常
- 下周期建议优先处理什么

### 4.4 半自动决策

Agent 定时生成建议，但不直接执行高风险动作：

- 高风险客户跟进建议
- 合同逾期处理建议
- 项目延期风险清单
- 待人工确认的操作计划

## 5. 非目标

第一阶段不建议做这些能力：

- 不支持无人确认的代码修改、部署、删除数据等高风险动作。
- 不做完全开放的后台 Agent，不允许绕过工具权限。
- 不把定时会话和现有 `scheduled_task` 表混用。
- 不要求第一版支持复杂多 Agent 协作。

## 6. 总体架构

新增能力建议放在 `agent-server`，因为定时会话依赖工作台会话、LLM、工具注册表、消息仓储和模式系统。

建议模块：

```text
agent-server
  service
    scheduled_agent_task_service.go
    scheduled_agent_runner.go
  model
    scheduled_agent_task.go
    scheduled_agent_execution.go
  repository
    scheduled_agent_task_repository.go
    scheduled_agent_execution_repository.go
  server
    scheduled_agent_task_handler.go
```

执行链路：

```text
用户创建 Agent 自动任务
  -> 写入 scheduled_agent_task
  -> agent-server scheduler 轮询到期任务
  -> 抢占 lease
  -> 创建 scheduled_agent_execution
  -> 创建工作台 chat_session
  -> 写入 user message
  -> 调用非 SSE 的 WorkspaceChat Runner
  -> 记录 assistant/tool messages
  -> 更新 execution 状态和摘要
  -> 按策略通知用户
```

## 7. 数据模型

### 7.1 scheduled_agent_task

建议字段：

```sql
id
name
full_code_path
goal
mode_code
files
context_policy
tool_policy
approval_policy
budget_policy
schedule_type
run_at
cron_expr
interval_seconds
max_runs
timezone
next_run_at
status
run_count
last_session_id
last_execution_id
last_error_message
created_by
request_user
request_user_dept
notify_users
notify_departments
notify_on
lease_owner
lease_until
created_at
updated_at
deleted_at
```

字段说明：

- `goal`：定时会话每次执行的目标，例如“每天检查项目风险并生成摘要”。
- `mode_code`：复用工作台模式，例如 `execute`、`dev`、`modify`。第一阶段建议默认只开放低风险模式。
- `files`：可选文件引用，和当前工作台文件引用体系保持一致。
- `context_policy`：控制是否加载历史会话、项目摘要、最近执行结果等上下文。
- `tool_policy`：工具白名单和写权限边界。
- `approval_policy`：是否需要人工确认高风险工具调用。
- `budget_policy`：token、工具轮数、执行时长限制。
- `last_session_id`：方便用户直接打开最近一次定时会话。

### 7.2 scheduled_agent_execution

建议字段：

```sql
id
task_id
session_id
scheduled_at
started_at
finished_at
status
duration_millis
input_goal
output_summary
tool_call_count
token_usage
error_message
trace_id
created_at
updated_at
```

状态建议：

- `pending`
- `running`
- `success`
- `failed`
- `timeout`
- `cancelled`
- `approval_required`

## 8. API 设计

建议新增独立接口，不复用现有 `/scheduled_tasks`。

```text
POST   /workspace/api/v1/scheduled_agent_tasks
GET    /workspace/api/v1/scheduled_agent_tasks
GET    /workspace/api/v1/scheduled_agent_tasks/:id
PUT    /workspace/api/v1/scheduled_agent_tasks/:id
DELETE /workspace/api/v1/scheduled_agent_tasks/:id
POST   /workspace/api/v1/scheduled_agent_tasks/:id/run
POST   /workspace/api/v1/scheduled_agent_tasks/:id/pause
POST   /workspace/api/v1/scheduled_agent_tasks/:id/resume
GET    /workspace/api/v1/scheduled_agent_tasks/:id/executions
GET    /workspace/api/v1/scheduled_agent_tasks/:id/executions/:execution_id
```

创建请求示例：

```json
{
  "name": "每日项目巡检",
  "full_code_path": "/department/demo/project",
  "goal": "每天检查项目中的异常数据、待处理事项和图表渲染情况，输出风险摘要。",
  "mode_code": "execute",
  "schedule_type": "cron",
  "cron_expr": "0 9 * * *",
  "timezone": "Asia/Shanghai",
  "tool_policy": {
    "allowed_tools": [
      "list_tables",
      "query_table",
      "get_workspace_project",
      "list_scheduled_tasks"
    ],
    "allow_write": false,
    "allow_deploy": false
  },
  "budget_policy": {
    "max_duration_seconds": 300,
    "max_tool_rounds": 20,
    "max_tokens": 8000
  },
  "notify_users": ["admin"],
  "notify_on": "failed"
}
```

## 9. 工作台会话复用方案

当前工作台入口偏 SSE 流式输出。定时会话没有浏览器连接，因此建议把核心执行逻辑抽成可复用 runner。

建议重构目标：

```go
type WorkspaceChatEventSink interface {
    Send(event string, data interface{})
}

func (s *WorkspaceChatService) RunWorkspaceChat(
    ctx context.Context,
    req *dto.WorkspaceChatReq,
    sink WorkspaceChatEventSink,
) error
```

两种 sink：

- `SSEEventSink`：给现有前端聊天使用。
- `BackgroundEventSink`：给定时会话使用，写执行摘要、工具调用统计和错误。

这样可以避免复制 Agent 执行逻辑，也能让定时会话天然复用已有消息表、会话表和工具调用记录。

## 10. 调度器设计

可以参考现有定时任务调度器的思路，但数据表和服务独立。

核心能力：

- 定时轮询 `next_run_at <= now` 的任务
- 批量拉取
- lease 抢占，避免多实例重复执行
- 并发限制
- 超时控制
- 成功后计算下一次执行时间
- 失败后记录错误并按策略通知

调度参数建议：

```text
poll_interval: 5s
batch_size: 20
lease_duration: 10m
max_concurrency: 3
default_timeout: 5m
```

第一阶段不建议并发太高，因为 Agent 执行会消耗 LLM 和工具资源。

## 11. 权限和安全

定时会话是无人值守执行，权限必须比普通工作台更严格。

第一阶段建议默认策略：

- 默认只读工具。
- 默认不允许部署。
- 默认不允许删除数据。
- 默认不允许批量更新业务数据。
- 写操作必须显式打开。
- 高风险工具调用必须转人工确认。
- 每个任务必须有最大执行时长。
- 每个任务必须有最大工具轮数。
- 每个任务可以配置 token 预算。

工具风险级别建议：

| 风险级别 | 示例 | 策略 |
| --- | --- | --- |
| 低 | 查询表、读取项目、读取配置 | 可默认允许 |
| 中 | 写表、创建待办、发送通知 | 需要显式授权 |
| 高 | 删除数据、部署、改代码、执行命令 | 第一阶段禁止或必须审批 |

## 12. 前端设计

建议新增统一入口：`自动化`。

入口下分两个 Tab：

- `函数任务`
- `Agent 任务`

列表字段建议：

- 名称
- 类型
- 所属项目
- 触发规则
- 下次执行时间
- 最近状态
- 最近执行耗时
- 运行次数
- 操作：查看、立即运行、暂停、恢复、删除

Agent 任务卡片需要显式展示：

- 会消耗 token
- 智能执行，结果可能变化
- 工具权限范围
- 是否需要人工确认
- 最近会话入口

创建 Agent 任务的表单建议分步：

1. 基本信息：名称、项目、目标描述、模式。
2. 触发规则：一次性、cron、间隔。
3. 能力范围：工具白名单、是否允许写操作。
4. 安全预算：超时、工具轮数、token 上限。
5. 通知策略：成功、失败、需要审批时通知谁。

## 13. Agent 辅助创建

Agent 可以帮助用户从自然语言生成任务草案。

用户输入：

> 每天上午 9 点帮我检查这个项目有没有异常数据和待处理事项，有问题通知我。

Agent 输出任务草案：

- 类型：Agent 任务
- 目标：检查异常数据、待处理事项并输出摘要
- 频率：每天 9 点
- 工具权限：只读
- 通知策略：失败或发现风险时通知创建人

用户确认后再创建任务。

这比让用户手填 cron 和工具策略更友好。

## 14. 和现有定时任务的集成边界

可以复用：

- 触发类型概念：`atime / cron / every`
- 下一次执行时间计算思路
- lease 抢占思路
- 执行记录列表交互
- 通知策略模型

不复用：

- 不复用 `scheduled_task` 表
- 不复用 `scheduled_task_execution` 表
- 不复用 `action` 字段扩展 Agent 行为
- 不让 app-server 调用 LLM 执行 Agent 会话

边界原则：

> 函数任务归 app-server，Agent 任务归 agent-server。前端统一展示，后端职责分离。

## 15. 实施阶段

### 第一阶段：MVP

目标：能创建、运行、查看 Agent 定时会话。

范围：

- 新增 `scheduled_agent_task` 表
- 新增 `scheduled_agent_execution` 表
- 新增 agent-server 调度器
- 抽出非 SSE 工作台执行入口
- 支持 `atime / cron / every`
- 支持只读工具白名单
- 支持执行记录和关联会话
- 支持失败通知

限制：

- 默认不允许写操作
- 默认不允许部署
- 默认不支持审批流
- 默认并发较低

### 第二阶段：治理增强

目标：让定时会话可控地处理半自动业务任务。

范围：

- 工具风险分级
- 人工确认
- token 预算
- 执行摘要
- 异常报告
- 手动重跑
- 任务模板

### 第三阶段：自动化编排

目标：把函数任务和 Agent 任务组合起来。

范围：

- 函数任务触发 Agent 分析
- Agent 生成函数任务配置
- 异常发现后自动创建待办
- 多步骤自动化流程
- 运行质量统计

## 16. 风险

### 16.1 Agent 输出不稳定

应对：

- 第一阶段只开放分析、总结、巡检类场景。
- 默认只读工具。
- 输出结构化摘要。

### 16.2 成本不可控

应对：

- 设置最大工具轮数。
- 设置最大执行时间。
- 设置 token 预算。
- 前端明确展示“会消耗 token”。

### 16.3 无人值守误操作

应对：

- 默认禁止高风险工具。
- 写操作必须显式授权。
- 高风险动作转人工确认。

### 16.4 重复执行

应对：

- lease 机制。
- 单任务并发锁。
- execution 幂等记录。

### 16.5 和现有定时任务概念混淆

应对：

- 产品入口分类型。
- 列表展示成本、风险、执行对象。
- 创建时明确选择“函数任务”或“Agent 任务”。

## 17. 验收标准

MVP 验收标准：

- 可以创建 Agent 自动任务。
- 可以按一次性、cron、间隔三种方式触发。
- 每次触发都会生成 execution。
- 每次 execution 都关联一个工作台会话。
- 用户可以从执行记录打开会话。
- 任务失败会记录错误。
- 同一任务不会并发重复执行。
- 默认工具权限为只读。
- 前端能明确区分函数任务和 Agent 任务。

## 18. 结论

定时会话有必要做，但不应该和现有定时任务耦合。

推荐方案是：

> 保留现有定时任务作为确定函数调度；新增 Agent 自动任务作为智能定时会话。两者前端统一在“自动化”里展示，后端表结构、执行器和权限模型保持独立。

这样既能保持现有定时任务稳定可控，又能扩展出项目巡检、异常分析、风险总结、半自动决策等更高价值的自动化能力。
