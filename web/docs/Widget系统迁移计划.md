# Widget 系统迁移计划：从 widgets/ 到 widgets-v2/

## 🎯 迁移目标

**完全迁移到 widgets-v2，废弃 widgets/（v1）**

- ✅ 所有场景使用 widgets-v2 组件
- ✅ 删除旧版本 widgets/ 目录
- ✅ 保证功能可用性
- ✅ 支持回滚

## 📊 迁移点清单

### 1. 核心使用点（必须迁移）

#### 1.1 TableRenderer.vue
**文件**：`web/src/components/TableRenderer.vue`

**当前使用**：
- `WidgetBuilder.createTemporary()` - 创建临时 Widget
- `widget.renderTableCell()` - 渲染表格单元格
- `widget.renderForDetail()` - 渲染详情展示

**迁移位置**：
- 第 263 行：`import { WidgetBuilder } from '@/core/factories/WidgetBuilder'`
- 第 537 行：`renderTableCell()` 函数
- 第 596 行：`renderDetailField()` 函数

**迁移方案**：
- 使用 `widgetComponentFactory.getRequestComponent()` 获取组件
- 使用 `<component :is="..." mode="table-cell" />` 渲染
- 使用 `<component :is="..." mode="detail" />` 渲染详情

#### 1.2 SearchInput.vue
**文件**：`web/src/components/SearchInput.vue`

**当前使用**：
- `WidgetBuilder.createTemporary()` - 创建临时 Widget
- `widget.renderSearchInput()` - 获取搜索输入配置

**迁移位置**：
- 第 163 行：`import { WidgetBuilder } from '@/core/factories/WidgetBuilder'`
- 第 332-355 行：`inputConfig` computed

**迁移方案**：
- 直接使用 widgets-v2 组件，`mode="search"`
- 或者创建适配层，从组件中提取配置

### 2. 内部使用点（需要检查）

#### 2.1 ResponseTableWidget.ts
**文件**：`web/src/core/widgets/ResponseTableWidget.ts`

**当前使用**：
- 内部使用 `WidgetBuilder.createTemporary()` 和 `renderTableCell()`

**迁移方案**：
- 如果 ResponseTableWidget 还在使用，需要迁移
- 如果已经废弃，可以删除

#### 2.2 ResponseFormWidget.ts
**文件**：`web/src/core/widgets/ResponseFormWidget.ts`

**当前使用**：
- 内部使用旧版本 Widget

**迁移方案**：
- 检查是否还在使用
- 如果使用，迁移到 widgets-v2

### 3. 工厂和工具（需要检查）

#### 3.1 WidgetFactory.ts
**文件**：`web/src/core/factories/WidgetFactory.ts`

**迁移方案**：
- 检查是否还有其他地方使用
- 如果没有，可以删除

#### 3.2 WidgetBuilder.ts
**文件**：`web/src/core/factories/WidgetBuilder.ts`

**迁移方案**：
- 检查是否还有其他地方使用
- 如果没有，可以删除

## 🔄 迁移策略

### 策略：渐进式迁移 + 功能开关

**原则**：
1. **先迁移，再删除**：先完成迁移，验证功能，再删除旧代码
2. **功能开关**：使用环境变量或配置开关，支持快速回滚
3. **逐步验证**：每个迁移点完成后立即验证
4. **保留备份**：迁移前创建分支，支持回滚

### 迁移步骤

#### Phase 1: 准备阶段（1-2小时）

1. **创建迁移分支**
   ```bash
   git checkout -b migrate/widgets-v2
   git push -u origin migrate/widgets-v2
   ```

2. **添加功能开关**
   - 在配置文件中添加 `USE_WIDGETS_V2` 开关
   - 默认使用新版本，支持快速回滚

3. **创建适配层（可选）**
   - 为 widgets-v2 创建适配函数
   - 兼容现有代码接口

#### Phase 2: 迁移 TableRenderer（2-3小时）

1. **迁移 renderTableCell**
   - 替换 `WidgetBuilder.createTemporary()` 为 `widgetComponentFactory.getRequestComponent()`
   - 使用 `<component :is="..." mode="table-cell" />` 渲染
   - 处理 VNode 渲染

2. **迁移 renderDetailField**
   - 替换为使用 widgets-v2 组件
   - 使用 `mode="detail"`

3. **测试验证**
   - 测试表格渲染
   - 测试详情展示
   - 测试所有 Widget 类型

#### Phase 3: 迁移 SearchInput（1-2小时）

1. **迁移 inputConfig**
   - 方案A：直接使用 widgets-v2 组件（推荐）
   - 方案B：创建适配层，提取配置

2. **测试验证**
   - 测试所有搜索类型（eq, like, in, gte/lte）
   - 测试所有 Widget 类型

#### Phase 4: 清理和验证（1-2小时）

1. **检查其他使用点**
   - 检查 ResponseTableWidget、ResponseFormWidget 是否还在使用
   - 如果使用，迁移或删除

2. **删除旧代码**
   - 删除 `web/src/core/widgets/` 目录
   - 删除 `web/src/core/factories/WidgetFactory.ts`
   - 删除 `web/src/core/factories/WidgetBuilder.ts`

3. **全面测试**
   - 测试所有功能
   - 测试所有 Widget 类型
   - 测试边界情况

#### Phase 5: 文档和提交（0.5小时）

1. **更新文档**
   - 更新 README
   - 更新架构文档

2. **提交代码**
   ```bash
   git add -A
   git commit -m "feat: 完全迁移到 widgets-v2，废弃 widgets/ v1"
   git push
   ```

## 🛡️ 风险控制

### 1. 回滚方案

**方案A：Git 回滚**
```bash
# 如果迁移失败，回滚到迁移前
git reset --hard <迁移前commit>
git push --force
```

**方案B：功能开关**
```typescript
// 在配置中添加开关
const USE_WIDGETS_V2 = import.meta.env.VITE_USE_WIDGETS_V2 !== 'false'

// 在代码中使用
if (USE_WIDGETS_V2) {
  // 使用新版本
} else {
  // 使用旧版本（回滚）
}
```

### 2. 测试清单

**功能测试**：
- [ ] 表格渲染正常
- [ ] 表格单元格显示正常（所有 Widget 类型）
- [ ] 详情展示正常
- [ ] 搜索输入正常（所有搜索类型）
- [ ] 用户选择器正常
- [ ] 文件上传正常
- [ ] 嵌套结构正常（TableWidget、FormWidget）

**边界测试**：
- [ ] 空值处理
- [ ] 错误处理
- [ ] 性能测试（大量数据）

### 3. 兼容性检查

**需要检查的 Widget 类型**：
- [ ] input
- [ ] number
- [ ] float
- [ ] text_area
- [ ] select
- [ ] multiselect
- [ ] switch
- [ ] timestamp
- [ ] files
- [ ] user
- [ ] table（嵌套）
- [ ] form（嵌套）

## 📝 迁移检查清单

### 迁移前
- [ ] 代码已提交并推送
- [ ] 创建迁移分支
- [ ] 添加功能开关
- [ ] 备份当前代码

### 迁移中
- [ ] TableRenderer 迁移完成
- [ ] SearchInput 迁移完成
- [ ] 所有功能测试通过
- [ ] 性能测试通过

### 迁移后
- [ ] 删除旧代码
- [ ] 更新文档
- [ ] 代码审查
- [ ] 合并到主分支

## 🚀 开始迁移

### 第一步：创建迁移分支

```bash
# 确保当前代码已提交
git status

# 创建迁移分支
git checkout -b migrate/widgets-v2
git push -u origin migrate/widgets-v2
```

### 第二步：添加功能开关（可选）

在 `web/src/core/config.ts` 或环境变量中添加：

```typescript
export const USE_WIDGETS_V2 = true // 默认使用新版本
```

### 第三步：开始迁移

按照 Phase 2-5 逐步迁移。

## 📚 参考文档

- `web/docs/新旧版本Widget系统深度对比分析.md` - 对比分析
- `web/docs/迁移TableRenderer到widgets-v2方案.md` - 迁移方案
- `web/src/core/widgets-v2/types.ts` - 新版本类型定义
- `web/src/core/factories-v2/WidgetComponentFactory.ts` - 新版本工厂

## ⚠️ 注意事项

1. **不要同时修改多个文件**：一次迁移一个文件，验证后再继续
2. **保留旧代码**：迁移完成并验证后再删除
3. **充分测试**：每个迁移点完成后立即测试
4. **文档更新**：及时更新相关文档

