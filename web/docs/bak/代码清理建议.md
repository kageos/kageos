# 代码清理建议

## 概述

经过代码审查，发现以下可以优化的点：

## 1. 旧版本渲染器（可删除）

### `web/src/core/renderers/FormRenderer.vue`

**状态**：只在测试页面使用

**使用位置**：
- `web/src/views/Test/FormRendererTest.vue` - 测试页面

**建议**：
- ✅ **可以删除**：测试页面可以迁移到使用新版本的 `renderers-v2/FormRenderer.vue`
- 或者保留测试页面，但更新为使用新版本

### `web/src/components/LegacyFormRenderer.vue`

**状态**：未被使用

**使用位置**：
- 无（grep 未找到任何引用）

**建议**：
- ✅ **可以删除**：完全没有被使用

## 2. 旧版本 Widgets（保留）

### `web/src/core/widgets/` 目录

**状态**：仍在生产代码中使用

**使用位置**：
- `web/src/components/TableRenderer.vue` - 通过 `WidgetFactory` 使用
- `web/src/components/SearchInput.vue` - 可能通过 `WidgetFactory` 使用
- `web/src/core/widgets/UserWidget.ts` - 在 TableRenderer 中使用

**建议**：
- ⚠️ **暂时保留**：这些组件还在使用中，不能直接删除
- 🔄 **迁移计划**：需要逐步迁移到 `widgets-v2/` 版本

## 3. 测试页面优化

### `web/src/views/Test/FormRendererTest.vue`

**当前状态**：使用旧版本的 `renderers/FormRenderer.vue`

**建议**：
- 🔄 **更新为使用新版本**：`renderers-v2/FormRenderer.vue`
- 这样可以统一使用新版本，并可以删除旧版本

## 4. 文档清理

### 过时的文档

**建议**：
- 检查并更新 `web/src/core/README.md`，确保指向正确的文件
- 更新其他相关文档，确保指向新版本

## 5. 清理步骤

### 步骤1：更新测试页面

```typescript
// web/src/views/Test/FormRendererTest.vue
// 从
import FormRenderer from '@/core/renderers/FormRenderer.vue'
// 改为
import FormRenderer from '@/core/renderers-v2/FormRenderer.vue'
```

### 步骤2：删除未使用的文件

```bash
# 删除 LegacyFormRenderer（未被使用）
rm web/src/components/LegacyFormRenderer.vue

# 删除旧版本 FormRenderer（测试页面已迁移后）
rm web/src/core/renderers/FormRenderer.vue
```

### 步骤3：更新文档

- 更新 `web/src/core/README.md`
- 更新其他相关文档

## 6. 注意事项

⚠️ **重要**：
- 不要删除 `web/src/core/widgets/` 目录，因为还在使用中
- 删除前确保测试页面已迁移到新版本
- 建议先提交当前的重构代码，然后再进行清理

## 7. 后续优化

### 长期计划

1. **迁移 TableRenderer 和 SearchInput**：
   - 逐步迁移到使用 `widgets-v2/` 版本
   - 完成后可以删除 `widgets/` 目录

2. **统一使用新版本**：
   - 所有组件都使用 `widgets-v2/` 和 `renderers-v2/`
   - 删除所有旧版本代码

3. **代码组织优化**：
   - 考虑重命名 `widgets-v2` 为 `widgets`（迁移完成后）
   - 考虑重命名 `renderers-v2` 为 `renderers`（迁移完成后）

