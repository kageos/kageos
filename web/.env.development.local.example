# 前端开发环境 - 本地复制为 .env.development.local 后按需修改
# cp .env.development.local.example .env.development.local

# ========== 方式一：连线上后端（只跑前端，API 走你提供的线上地址）==========
# 设置后，npm run dev 时所有 /api、/hub/api 等请求会通过 Vite 代理转发到下面地址，无需本地起后端。
# 注意：不要带末尾斜杠；需是网关统一入口（能转发到 app-server、hub、storage 等）。
# VITE_PROXY_TARGET=你的线上主站地址（主站+API 统一入口，不要末尾斜杠）
# 示例：
# VITE_PROXY_TARGET=https://kageos.example.com

# 若线上 MinIO 有单独代理路径，可设置（可选，不设则用本地 9000）
# VITE_MINIO_PROXY_TARGET=https://你的线上MinIO代理地址

# WebSocket 地址（若用实时功能，改成线上 WS 地址）
# VITE_WS_URL=wss://你的线上域名/ws

# 产品内“查看文档”按钮的官网根地址。调试官网文档时可指向 Astro 预览：
# VITE_KAGEOS_DOCS_BASE_URL=http://127.0.0.1:4322

# ========== 方式二：本地后端（不设置 VITE_PROXY_TARGET）==========
# 不创建本文件，或留空 VITE_PROXY_TARGET，则代理目标为 http://localhost:9090。
# 需要本地先起好：Docker 基础设施 + 网关/后端（见项目根目录 deploy/ 或 README）。
