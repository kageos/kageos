# AI-Agent-OS 全方位项目说明

> **用途**：当你需要和其他大模型/AI 助手讨论本项目时，把这份文档喂给它，可以让它快速、完整地理解项目全貌。
> 涵盖：产品定位、设计理念、核心概念、技术架构、已实现能力、使用方式与流程、能力边界与限制、未来规划、团队与现状。

---

## 一、项目定位（一句话）

**AI-Agent-OS** 是一个 **AI 原生的 ToB 应用平台**。核心理念：**「所述即所得，描述即生成」**——用户通过自然语言描述业务需求，AI 自动生成并运行可用的业务系统（表单、表格、图表、文档、讨论区），无需手写前端与大部分后端代码。

- **目标用户**：企业/组织（ToB）——业务人员、运营、IT 等需要快速搭建内部系统、工具或轻应用的团队；也适用于小店/小机构等轻量级客户。
- **产出物**：不是对话记录或一次性结果，而是**可持续使用、可被他人操作、可沉淀为资产的业务应用**。
- **差异化**：在同一个平台内打通「服务树组织 + Form/Table/Chart 可执行函数 + Docs/Board 内容节点 + 权限/审批/评论/操作日志 + AI 工作台生成与执行 + Hub 应用市场」，而非单纯的表单工具或单纯的 AI 助手。
- **许可**：BSL 1.1（企业内部使用与为客户定制免费）。

---

## 二、设计理念

### 核心哲学：大道至简，大道致减

当前 AI 时代的软件开发困境：LLM 能快速生成代码，但每个应用都是独立的「孤岛」——不同的 UI、不同的权限、不同的数据模型。软件越多，学习成本和协作成本越高。

**AI-Agent-OS 的解答**：

> 把企业软件拆到底，来来回回就三件事：**填个表提交（表单）、管一批数据（表格）、看个统计图（图表）**。不管是工单、招聘、库存还是投票，本质都是这三种东西的组合，区别只在于字段和业务规则。

所以：

- **通用部分由平台统一做好**：UI 渲染、权限、审批、评论、操作日志、文件上传、搜索排序……平台全内置。
- **个性化部分留给用户定义**：字段、规则、统计，用户描述清楚就行，AI 帮生成。
- **统一带来的价值**：不管建了 5 个系统还是 50 个，用法都一样，不存在「又要学一个新软件」。

**核心特征**：可编排、自描述、可移植、可监控、可复制、原子性——所有生成的应用都挂在服务树上，自动拥有平台级能力。

### 思维转变：从过程导向到结果导向

用户不需要说「怎么做」（拖控件、配样式、写逻辑），只需要说「要什么」（管什么数据、什么规则、看什么结果）。

> 比喻：过去要自己学开车才能出行；现在告诉司机「我要去机场」，车就自动开过去了。

---

## 三、核心概念

### 3.1 服务树（Service Tree）

平台用**树形结构**组织所有资源，每个节点有类型和 `full_code_path`（全局唯一路径），用于权限、菜单、API 路由。


| 节点类型     | 说明       | 用途                     |
| ------------ | ---------- | ------------------------ |
| **package**  | 目录/包    | 菜单层级、分组           |
| **function** | 可执行函数 | 见下方函数类型           |
| **docs**     | 单篇文档   | 知识沉淀、规范说明       |
| **board**    | 讨论区     | 公告、变更记录、内部讨论 |

### 3.2 三种函数类型（核心）


| 类型              | 本质                     | 典型场景                                                         |
| ----------------- | ------------------------ | ---------------------------------------------------------------- |
| **Form（表单）**  | 一次输入 → 一次输出     | 提交数据得结果、PDF 转 Word、上传文件转格式、生成报告            |
| **Table（表格）** | 管理一批记录（增删改查） | 工单管理、客户管理、订单管理、库存管理、任何「管一批数据」的系统 |
| **Chart（图表）** | 数据可视化               | 饼图、折线图、柱状图等统计展示                                   |

这三种可自由组合。例如「工单管理系统」= Table（管工单）+ Chart（工单统计）。

### 3.3 Docs 和 Board

比 Form/Table/Chart 更简单，不需要 AI 生成，用户在目录里直接创建：

- **Docs**：写文档（操作手册、规范、知识沉淀）
- **Board**：讨论区（公告、方案评审、问题反馈、FAQ）

### 3.4 应用（App）

一个工作空间，对应一个 Go 项目与运行实例，其下挂服务树。每个应用独立运行在容器中，物理级别隔离。

### 3.5 full_code_path

节点/函数的全局唯一路径（如 `/luobei/myapp/task_management`），贯穿权限、API、存储等所有场景。

### 3.6 组件值（FieldValue）

每个字段的值包含三部分：

- **raw**：提交给后端的原始值
- **display**：前端展示值
- **meta**：元数据

提交时只取 raw 值。

---

## 四、技术架构

### 4.1 整体架构

```
用户/浏览器
  → Web 前端（Vue3 + Element Plus：工作空间、服务树、工作台对话、Form/Table/Chart 动态渲染）
  → API Gateway（按 path 分流）
  → app-server（应用、服务树、函数注册、Docs/Board、权限、历史等元数据）
  → agent-server（工作台会话、LLM、工具注册与编排、Prompt/文档）
  → app-runtime（容器化运行实例，基于 Podman，通过 NATS 调用 SDK 应用）
  → hr-server（用户、部门、认证）
  → app-storage（文件上传/下载/存储）
```

### 4.2 核心模块


| 模块             | 代码路径            | 职责                                                                  |
| ---------------- | ------------------- | --------------------------------------------------------------------- |
| **api-gateway**  | core/api-gateway    | 路由与反向代理                                                        |
| **app-server**   | core/app-server     | 应用、服务树、函数注册、Docs/Board CRUD、权限、历史等元数据           |
| **agent-server** | core/agent-server   | 工作台会话、LLM 调用、工具注册与编排（ToolRegistry）、Prompt/规范文档 |
| **app-runtime**  | core/app-runtime    | 应用容器实例管理（Podman）、NATS 通信、调用 SDK 应用                  |
| **app-storage**  | core/app-storage    | 统一文件上传/下载/存储                                                |
| **hr-server**    | core/hr-server      | 用户、部门、认证                                                      |
| **SDK**          | sdk/agent-app       | 业务开发包（Go）：app、callback、response、widget、模板注册           |
| **Web**          | web/                | 前端：工作空间、服务树、工作台、widgets-v2 组件、factories-v2 工厂    |
| **Hub**          | enterprise_impl/hub | 应用市场：发布、更新、复制（Fork）                                    |

### 4.3 技术栈

- **后端**：Go
- **前端**：Vue3 + Element Plus
- **数据库**：MySQL（主）
- **消息**：NATS
- **容器**：Podman
- **AI**：接大模型 API（支持多种 LLM）
- **部署**：Docker/Podman 部署方案就绪

---

## 五、已实现能力清单

### 5.1 平台横切能力（所有应用自动拥有，业务代码无需实现）


| 能力                   | 说明                                                                       |
| ---------------------- | -------------------------------------------------------------------------- |
| **权限管理**           | 按 full_code_path 管控，RBAC，支持字段级 permission 标签，目录权限自动继承 |
| **流程审批**           | Table 增删改、Form 提交均可配置审批策略（串签/并签/会签等）                |
| **评论 / 点赞 / 收藏** | 每个 Table/Form/记录自带                                                   |
| **操作日志**           | 平台自动记录（谁、什么时候、做了什么）                                     |
| **定时任务**           | 平台提供调度，业务无需自建 cron                                            |
| **搜索和排序**         | Table 自动支持                                                             |
| **文件上传**         | 文件/图片类字段自动支持拖拽上传、进度条、预览                              |
| **用户 / 组织**        | 使用 user 组件与系统字段，自动对接 HR                                      |

### 5.2 内置工具库（开箱即用）

平台在 **system 用户**下预置了大量可直接调用的 Form/Table/Chart 函数（路径：`namespace/system/official/code/api/`），覆盖常见场景：

- **文件处理类**：PDF 转 Word、Excel 解析、图片格式转换、视频转码、视频加水印、音频提取等
- **数据处理类**：Excel 生成 SQL、数据清洗、JSON/CSV 格式转换等
- **文本/图表类**：Markdown 转 HTML、数据可视化、折线图/饼图/柱状图生成等

**使用方式**：

- 工作台 AI 先按用户意图判断场景：文件/媒体/数据处理走官方工具文档包，平台接口走 OpenAPI 文档包。
- 系统注入对应文档后，再通过 `search_tools` 按关键词搜索可用工具（system 用户下已注册的 Form/Table/Chart 函数），找到后通过 `run_form_submit`、`run_table_search`、`run_chart_query` 等调用，结果（包括文件）直接输出给用户。
- 不传关键词时，返回按调用次数排序的高频函数。
- 多关键词用竖线 `|` 分隔（OR 语义），如 `折线图|chart|画图`。

**生态扩展**：

- 用户在自己的工作空间生成的任何 Form/Table/Chart 都会注册到服务树上，后续也可被 `search_tools` 搜到并调用。
- system 下的官方工具可持续扩展：平台方新增 `/system/tools` 或 `/system/openapi` 函数后，所有用户立即可用。
- Hub 上其他用户发布的应用，Fork 后同样可被搜索和调用。

这意味着：**平台上的每一个函数，无论是内置的、AI 生成的、还是从 Hub 复制的，都是可被搜索、可被调用、可被工作流串联的一等公民。** 函数越多，平台能力越强，这是一个正向飞轮。

### 5.3 智能工作台（Agent）

工作台是用户与 AI 协作的一站式入口，绑定当前目录：

**两大能力**：

1. **开发/修改**：读文档、写代码、建目录、编译（生成新系统 / 修改已有系统）
2. **执行/操作**：查表、提交表单、查图表、新增记录（用自然语言操作已有系统）；也可通过官方工具文档包和 `search_tools` 调用官方工具完成临时任务（如「帮我把这个 PDF 转成 Word」）

**工作模式**：

- **开发模式**（dev）：统一承接生成、修改、编译、操作测试和临时工具任务。

**主链路**：先识别用户意图，再由系统注入对应 SOP、SDK 主文档、平台规则、案例和校验文档，最后通过 tools 执行动作。

**工具链**：read_doc、read_go_file、write_go_file、search_replace_file、create_directory、build_workspace、run_table_search、run_form_submit、run_chart_query、web_search、**search_tools**、search_hub_directory、publish_to_hub 等

**规范**：创建类需求先由系统注入创建 SOP，输出 PRD → 用户确认 → 再写代码；修改类注入修改 SOP；执行类注入操作测试或官方工具文档包，再调用 run_* 工具。

### 5.4 多级组织与数据隔离

多级组织（如总部-区域-门店）直接用服务树目录表达：

```
永辉超市
├── 南京片区
│   ├── 新街口分店     ← 仅存本店数据
│   └── 玄武湖分店     ← 数据独立
└── 上海片区
    └── 陆家嘴分店     ← 数据独立
```

权限按目录申请且继承：申请「南京片区」→ 可看南京下所有门店。

### 5.5 统一 UI 渲染

- 后端定义组件类型（`sdk/agent-app/widget/`）
- 前端按 schema 动态渲染（`web/src/core/widgets-v2/components/`）
- 组件支持多种 mode：edit、response、table-cell、detail、search
- 所有应用统一 UI 风格、统一操作方式

### 5.6 Hub（应用市场）

- 发布应用到市场、其他用户一键复制（Fork）
- 支持免费分享或付费出售
- Fork = 物理隔离的完整副本，数据独立

### 5.7 容器化运行时

- 每个应用独立运行在 Podman 容器中
- 物理级别多租户隔离
- 极低资源占用（~10MB/应用）

### 5.8 预装工具（工作台环境）

FFmpeg、GraphicsMagick、ImageMagick、ExifTool、OCRmyPDF、libvips-tools、WebP tools、pngquant、gifsicle、unpaper、LibRaw、Ghostscript、Poppler、Tesseract、LibreOffice、Pandoc、Graphviz、Python3（含 pandas/numpy/matplotlib/jieba 等）

---

## 六、使用方式与流程

### 6.1 创建新功能

```
用户在工作台描述需求（自然语言）
  ↓
系统按开发意图注入创建 SOP 和 SDK 主文档
  ↓
AI 分析需求，输出方案/PRD（模块、字段、逻辑）
  ↓
用户确认（OK → 继续；不满意 → 告诉 AI 改哪里）
  ↓
AI 生成代码并编译（约 1 分钟）
  ↓
刷新页面，新功能可用
```

### 6.2 修改已有功能

在工作台里说：「帮我给工单管理加一个截止日期字段」「把状态改为已完成时自动计算耗时」等。AI 先读取 `sop.modify-project`，再修改代码、重新编译，刷新即可。

### 6.3 操作已有系统

在工作台里说：「帮我查一下所有待处理的工单」「帮我新增一条客户记录」「帮我看看这个月的统计」。AI 先读取 `sop.execute-function` 并确认 schema，再调用 run_table_search、run_form_submit 等工具返回结果。

### 6.4 用户只需要关心的三件事

1. **管什么数据**（有哪些字段）
2. **数据有什么规则**（状态流转、自动计算、必填校验等）
3. **想看什么结果**（统计图表、报告等）

界面样式、布局、交互、移动端适配等，全部由平台统一处理，用户不需要也无法自定义。

---

## 七、能力边界与限制

### 7.1 平台侧 vs 应用侧


|          | 平台负责（用户改不了）                                    | 应用负责（用户在工作台能改）           |
| -------- | --------------------------------------------------------- | -------------------------------------- |
| **范围** | UI 样式、组件交互、布局排版、移动端适配、文件上传控件行为 | 字段定义、业务逻辑、数据处理、统计规则 |
| **谁改** | 平台开发团队                                              | 用户描述需求，AI 生成                  |

### 7.2 擅长的（核心方向）

- **各种管理系统**：工单、客户、订单、库存、招聘、资产、证书、会议室预约……凡是「管一批数据 + 增删改查 + 统计」
- **各种工具**：PDF 转 Word、图片转换、Excel 解析、视频处理（借助 FFmpeg 等预装工具）……凡是「输入 → 处理 → 输出」
- **数据统计看板**：基于已有数据做饼图、折线图、柱状图等

**一句话判断**：本质上是「管数据」或「处理数据」→ 大概率能做。

### 7.3 做不了的


| 不适合做的           | 原因                                     |
| -------------------- | ---------------------------------------- |
| 直播平台、视频网站   | 需要实时流媒体传输                       |
| 聊天软件、即时通讯   | 需要实时双向通信（WebSocket）            |
| 在线视频/图片编辑器  | 需要复杂前端交互（拖拽、画布、实时预览） |
| 游戏、互动娱乐       | 完全不同的技术体系                       |
| 电商前台（消费者端） | 需要高度定制 UI、购物车、支付对接        |
| 远程桌面、在线终端   | 需要持续连接和实时交互                   |

**一句话判断**：本质上是「复杂交互」或「实时通信」→ 大概率做不了。

### 7.4 三个技术判断维度

评估任何需求，依次过：

1. **能不能用 Form/Table/Chart 承载？**（不能 → 做不了）
2. **有没有现成工具/库支撑？**（没有 → 做不了或需降级）
3. **复杂度是否在「一次请求-响应」范围内？**（超出 → 做不了）

三个都通过才能做。平台有定时任务横切能力，所以「定时 XX」可拆成「单次执行（Form/Table）+ 平台定时调度」。

### 7.5 动态字段限制

Form/Table 字段在代码编译时确定，不支持运行时动态生成不同字段。因此：

- 「通用问卷系统」（每份问卷字段不同）做不了通用版
- **降级方案**：① 每份问卷单独生成一个 Form/Table；② 用问卷星等专业工具做问卷，平台做管理和统计

### 7.6 统一渲染限制

所有应用共用一套 UI 风格，用户**不能**自定义布局、样式、颜色、字号、按钮大小等。这是设计选择（统一 = 低学习成本），不是缺陷。

---

## 八、未来规划

### 8.1 工作流

**核心思路**：平台上的每个 Form 函数都有明确的输入参数和输出参数（通过函数详情接口完全固定且明确），AI 可以把多个 Form 函数按输入/输出匹配**串行调用**，自然形成工作流。

**工作原理**：

1. **搜索**：AI 通过 `search_tools` 按关键词搜索平台上已有的 Form 函数
2. **匹配**：根据每个函数的输入/输出参数（详情接口返回 schema），分析是否能前后串联——前一个函数的输出能否作为下一个函数的输入
3. **补缺**：如果某个环节缺少对应函数（例如缺「视频加水印」），AI 自动生成一个新函数来补全工作流
4. **统一入口**：把所有需要用户输入的变量参数（如水印名称、广告秒数）统一到工作流入口层
5. **保存用例**：工作流可保存为「用例」，下次只需输入变化的参数即可快速执行

**完整示例（视频处理工作流）**：

```
用户说：我需要一个短剧视频处理工作流——上传视频后，自动转码为 mp4，提取音频生成字幕，嵌入字幕，加上 xxx 传媒水印，在第 30 秒插入广告视频。

AI 执行：
  search_tools("视频转码|视频水印|音频提取|字幕|广告") 
  → 找到已有函数：VideoConv、AudioExtract、Audio2Subtitle、SubtitleEmbed、WatermarkAdd、AdInsert
  → 分析输入输出参数能否串联 → 可以
  → 统一入口参数：原始视频文件、水印名称+位置、广告视频+插入秒数
  → 串行执行：VideoConv → AudioExtract → Audio2Subtitle → SubtitleEmbed → WatermarkAdd → AdInsert
  → 输出：处理好的视频文件

保存为「短剧（xxx 探案视频生成）」工作流：
  固定参数：水印 = xxx 传媒，广告 = 固定广告片段，插入秒数 = 30
  每次只需上传视频文件即可一键执行
  第一集：上传第一集视频 → 执行工作流
  第二集：上传第二集视频 → 执行工作流
```

**关键特性**：

- **每个环节独立可用**：工作流中的每个函数（如视频转码、加水印）单独拿出来也是一个完整的工具，日常也能单独用
- **量变引起质变**：Form 函数越多，能排列组合出的工作流越多；用户生成的、Hub 上共享的、系统内置的，都能成为工作流的一环
- **AI 可调整**：用户可以说「新增一个环节：视频末尾加上片尾字幕」或「删掉广告插入环节」，AI 快速调整工作流
- **可上架交易**：高质量的工作流可上传到 Hub，其他用户付费使用
- **跨领域通用**：不只是视频处理——科研（基因序列分析）、AI 训练（数据清洗 → SFT 数据对生成 → 校验）、内容生产等任何可拆成多步的流程都适用

### 8.2 视图/看板

可拖拽函数组成看板：

- 把 Table、Chart、Form 等拖到一个视图页面
- 输入筛选参数，一屏看尽某个维度的全部数据
- 场景：会议室大屏、门店数据看板、桌位点餐等

### 8.3 MCP（Model Context Protocol）

函数详情接口可返回 MCP 格式，让外部大模型直接理解并调用平台函数，从模型层打通 AI 能力。

### 8.4 组织架构增强

org 组件等在规划中，支持更完整的组织架构管理。

### 8.5 报表型 Docs

数据驱动的文档，Docs 节点可基于 Table/Chart 数据自动生成内容。

---

## 九、与竞品/同类产品的关系

### 9.1 vs 传统低代码（简道云、宜搭、明道云）


| 维度     | 传统低代码                 | AI-Agent-OS                            |
| -------- | -------------------------- | -------------------------------------- |
| 使用方式 | 手动拖拽字段、配置规则     | 自然语言描述，AI 自动生成              |
| 学习成本 | 中等（需学平台操作）       | 极低（会说话就会用）                   |
| 扩展方式 | 自己配置新字段/规则        | 跟 AI 说一句话                         |
| 代码能力 | 无（受限于平台组件和规则） | 有（生成真实 Go 代码，可实现复杂逻辑） |
| 统一性   | 每个应用风格可能不同       | 所有应用统一 UI、权限、操作方式        |

### 9.2 vs 个人 AI 助手（如 OpenClaw）


| 维度     | AI-Agent-OS                                       | 个人 AI 助手                                   |
| -------- | ------------------------------------------------- | ---------------------------------------------- |
| 目标用户 | 企业/组织（ToB）                                  | 个人                                           |
| 核心产出 | **可运行的应用**（系统/工具），可沉淀、可复用     | **执行动作**（发消息、改日历），不产出独立应用 |
| 做事方式 | 先有/先造系统 → 工作台操作系统 → 留下标准与沉淀 | 直接操作已有渠道，不择手段完成，不强调沉淀     |
| 价值取向 | **系统优先、沉淀优先**：越用越有可复用的资产      | **任务优先、搞定优先**：先把眼前事办成         |

### 9.3 vs Cursor / AI 编程工具

Cursor 帮开发者写代码；AI-Agent-OS 帮**不懂技术的人**直接生成可用系统，不需要看代码。定位不同：一个面向开发者，一个面向业务人员。

---

## 十、当前成就与完成度

- **架构已打通**：API Gateway → app-server / agent-server / app-runtime / hr-server / app-storage 分工明确，全链路串联。
- **三种函数类型齐备**：Form、Table、Chart 从 SDK → 注册 → 执行 → 前端渲染全链路可用。
- **内置案例**：单 Table、单 Form、多 Table、Table+Form、Table+Form+Chart（工单、招聘、会议室、投票、收银台等）。
- **Docs 与 Board 已上线**。
- **工作台 Agent 可用**：多模式、工具链完整、Prompt 与规范已沉淀。
- **Hub 可用**：发布、复制、更新。
- **部署方案就绪**：Docker/Podman。
- **文档与规范**：设计理念、代码生成规范、平台能力说明、用户指南、能力边界文档均已沉淀。

**待加强**：组织架构增强、工作流、视图/看板、MCP、行业模板与标杆案例。

---

## 十一、团队与现状

- **团队**：兼职小团队（3 人核心）。技术+产品（1 人，快手研发效能，前腾讯企业 IT）、对外/市场（1 人）、前端（1 人）。其他伙伴在银行、京东等，可参与但非核心。
- **阶段**：产品可用但尚无付费客户；合作伙伴在试用中。
- **计划**：选定垂直行业（如美发/美容/教育等生活服务小店）→ 做行业模板 → 找种子客户 → 验证付费意愿 → 再扩量。
- **商业模式**：SaaS 优先（年付），按用户数/应用数阶梯收费；Hub 后期交易抽成；本地部署作为可选高级服务。

---

## 十二、关键文档导航


| 目的                   | 文档路径                                                                                                            |
| ---------------------- | ------------------------------------------------------------------------------------------------------------------- |
| 快速建立整体认知       | `blueprint/项目与平台能力总览-供大模型阅读.md`                                                                      |
| 设计哲学               | `blueprint/00-设计理念.md`                                                                                          |
| 系统架构图             | `blueprint/02-系统架构图.md`                                                                                        |
| 代码生成规范           | `blueprint/05-代码生成规范.md`                                                                                      |
| 能力边界               | `core/agent-server/prompt/system/prompt/platform-capability-boundaries.md`                                          |
| 用户指南（非技术人员） | `blueprint/产品使用指南-给非技术人员.md`                                                                            |
| 工作台 Prompt 与规范   | `core/agent-server/prompt/content/` 下各 mode                                                                       |
| 未来展望               | `note/todos/未来展望.md`                                                                                            |
| 商业化打法             | `note/临时分析/商业化打法与落地策略.md`                                                                             |
| 部署                   | `deploy/server-deploy.sh`、`podman/DEPLOY.md`                                                                       |
