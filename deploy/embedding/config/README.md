# Embedding — 配置说明

应用 YAML 已统一到 **`deploy/config/dev`** / **`deploy/config/prod`**。

本机覆盖请使用 **`deploy/config/local/`**，并在仓库根执行：

```bash
bash deploy/embedding/scripts/embedding.sh local
```

详见 **[../../deploy/config/README.md](../../deploy/config/README.md)**。
