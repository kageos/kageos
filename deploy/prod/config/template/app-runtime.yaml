runtime:
  port: 9093
  listen_host: "127.0.0.1"
  log_level: "info"
  debug: false

timeouts:
  app_server_request: 30
  container_startup: 2
  container_cleanup: 10

container:
  timeout: 30
  image:
    base_image: "${APP_BASE_IMAGE}"
