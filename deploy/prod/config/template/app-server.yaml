# App Server（客户）
server:
  port: 9091
  listen_host: "127.0.0.1"
  log_level: "info"
  debug: false
  enable_pprof: false

timer_worker:
  max_concurrency: 4

db:
  type: "mysql"
  host: "127.0.0.1"
  port: 3306
  user: "root"
  password: "${MYSQL_ROOT_PASSWORD}"
  name: "app_db"
  max_idle_conns: 30
  max_open_conns: 200
  max_lifetime: 300
  log_level: "warn"
  slow_threshold: 200

scheduled_task_db:
  name: "app-scheduled-task"

timeouts:
  app_request: 300
  nats_request: 300
