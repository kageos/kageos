# 本文件 = Compose 唯一配置源：复制为 .env 后按环境填写（或由他处直接生成/下发到 deploy/customer/.env）
# 无 YAML、无渲染步骤；变量名与下方一致即可。

CANONICAL_BASE_URL=
MYSQL_ROOT_PASSWORD=
MINIO_ROOT_USER=
MINIO_ROOT_PASSWORD=
JWT_SECRET=
CONTROL_ENC_KEY=
SMTP_PASSWORD=
HTTP_PUBLISH_PORT=
MAIN_IMAGE=
