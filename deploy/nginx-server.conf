# =============================================================================
# 本文件已迁至 Embedding 部署目录，与 embedding.sh、前后端静态路径维护放在一起：
#
#   deploy/embedding/nginx/nginx-server.conf
#
# 请编辑该文件。部署脚本（embedding.sh / server-deploy.sh）已引用新路径。
# =============================================================================
