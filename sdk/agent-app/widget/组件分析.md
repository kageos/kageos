# Widget 组件现状

本文档用于快速对齐 `sdk/agent-app/widget` 当前实际支持的组件能力，避免继续参考过期结论。

## 当前已支持的组件

| 组件类型 | 文件 | 常用配置 | 说明 |
|----------|------|----------|------|
| `ID` | `id.go` | — | 只读主键/标识；类型值区分大小写，必须写 `ID` |
| `input` | `input.go` | `placeholder`、`password`、`prepend`、`append`、`default` | 单行文本输入 |
| `text` | `text.go` | `format` | 只读文本展示，支持 `json/yaml/xml/markdown/html/csv` 等 |
| `text_area` | `text_area.go` | `placeholder`、`default` | 多行文本输入 |
| `richtext` | `richtext.go` | `height` | 富文本编辑 |
| `select` | `select.go` | `options`、`options_colors`、`placeholder`、`default`、`creatable` | 下拉单选 |
| `radio` | `radio.go` | `options`、`default` | 单选按钮 |
| `checkbox` | `checkbox.go` | `options`、`default` | 固定选项复选 |
| `multiselect` | `multiselect.go` | `options`、`options_colors`、`placeholder`、`default`、`max_count`、`creatable` | 下拉多选 |
| `number` | `number.go` | `placeholder`、`step`、`default`、`unit` | 整数输入 |
| `float` | `float.go` | `placeholder`、`precision`、`step`、`default`、`unit` | 小数输入 |
| `slider` | `slider.go` | `min`、`max`、`step`、`default`、`unit` | 可编辑滑块 |
| `rate` | `rate.go` | `max`、`allow_half`、`default`、`texts` | 星级评分 |
| `switch` | `switch.go` | — | 布尔开关；当前没有额外 config 字段 |
| `timestamp` | `timestamp.go` | `format`、`default`、`disabled` | 日期时间；值必须是毫秒时间戳 |
| `color` | `color.go` | `format`、`default`、`show_alpha` | 颜色选择 |
| `files` | `files.go` | `accept`、`max_size`、`max_count` | 文件上传/下载 |
| `user / users` | `user.go` / `users.go` | `default`、`disabled`、`max_count` | 用户选择 |
| `department / departments` | `department.go` / `departments.go` | `default`、`max_count` | 部门选择 |
| `progress` | `progress.go` | `min`、`max`、`unit` | 只读进度展示 |
| `link` | `link.go` | `text`、`target`、`type`、`icon` | 只读跳转链接 |
| `table` / `form` | `decode.go` / `widget.go` | — | 嵌套结构体容器 |

## 关键约定

- `type` 大小写敏感，主键展示写 `type:ID`，不要写成 `type:id`。
- 当前没有独立 `date`、`datetime`、`time`、`range`、`image`、`tag`、`tree`、`cascader`、`code` 等组件；日期时间统一使用 `timestamp + format`。
- `switch` 当前不支持 `default`、`true_label`、`false_label` 这类配置；布尔默认值应由业务逻辑或数据库默认值处理。
- `text`、`progress`、`link`、`ID` 更适合只读字段或响应字段。
- `table` / `form` 只支持具名 `struct` 或 `[]struct` 递归解析，不支持 `map[string]interface{}`。
- 数据类型由 Go 字段类型推断，不依赖 widget type；详见 `decode.go` 中的 `inferDataType`。

## 文档同步建议

- 对外提示词文档以 `core/agent-server/prompt/system/prompt/sdk/agent-app-sdk-readme.md` 为准。
- 新增、删除或调整组件配置时，至少同时更新以下三处：
  - `sdk/agent-app/widget/widget.go`
  - `core/agent-server/prompt/system/prompt/sdk/agent-app-sdk-readme.md`
  - 本文档
