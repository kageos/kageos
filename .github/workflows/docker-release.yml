name: Docker Release

on:
  push:
    tags:
      - "v*.*.*"
  workflow_dispatch:
    inputs:
      version:
        description: "Image version, for example 0.2.0. A leading v is accepted."
        required: true
        default: "0.2.0"
      push_latest:
        description: "Also update qiayanai/kageos:latest"
        required: true
        type: boolean
        default: true

permissions:
  contents: read

env:
  IMAGE_NAME: docker.io/qiayanai/kageos

jobs:
  metadata:
    name: Resolve release metadata
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.release.outputs.version }}
      push_latest: ${{ steps.release.outputs.push_latest }}
    steps:
      - name: Resolve version
        id: release
        shell: bash
        env:
          INPUT_VERSION: ${{ inputs.version }}
          INPUT_PUSH_LATEST: ${{ inputs.push_latest }}
        run: |
          set -euo pipefail

          if [[ "${GITHUB_REF_TYPE}" == "tag" ]]; then
            version="${GITHUB_REF_NAME#v}"
            push_latest="true"
          else
            version="${INPUT_VERSION}"
            version="${version#v}"
            push_latest="${INPUT_PUSH_LATEST}"
          fi

          if [[ ! "$version" =~ ^[0-9]+\.[0-9]+\.[0-9]+([-.][0-9A-Za-z][0-9A-Za-z.-]*)?$ ]]; then
            echo "version must look like 0.2.0, got: $version" >&2
            exit 1
          fi

          {
            echo "version=${version}"
            echo "push_latest=${push_latest}"
          } >> "$GITHUB_OUTPUT"

  build:
    name: Build ${{ matrix.platform }}
    needs: metadata
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: linux/amd64
            runner: ubuntu-24.04
            suffix: amd64
          - platform: linux/arm64
            runner: ubuntu-24.04-arm
            suffix: arm64
    runs-on: ${{ matrix.runner }}
    timeout-minutes: 180
    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Log in to Docker Hub
        uses: docker/login-action@v4
        with:
          username: ${{ vars.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Wait for matching Kagebase architecture
        shell: bash
        timeout-minutes: 150
        env:
          RELEASE_VERSION: ${{ needs.metadata.outputs.version }}
          REQUIRED_PLATFORM: ${{ matrix.platform }}
        run: |
          set -euo pipefail

          image="docker.io/qiayanai/kagebase:${RELEASE_VERSION}"
          for attempt in $(seq 1 120); do
            if manifest="$(docker buildx imagetools inspect "$image" 2>/dev/null)" &&
              grep -q "Platform:.*${REQUIRED_PLATFORM}" <<<"$manifest"; then
              printf '%s\n' "$manifest"
              exit 0
            fi
            echo "Waiting for ${image} ${REQUIRED_PLATFORM} (${attempt}/120)"
            sleep 60
          done

          echo "Matching Kagebase architecture was not published in time." >&2
          exit 1

      - name: Build and push native architecture image
        uses: docker/build-push-action@v7
        with:
          context: .
          file: deploy/aio/Dockerfile
          platforms: ${{ matrix.platform }}
          push: true
          # Keep the manifest list portable to registries that do not support
          # Buildx attestation manifests, including Aliyun ACR.
          provenance: false
          sbom: false
          tags: ${{ env.IMAGE_NAME }}:${{ needs.metadata.outputs.version }}-${{ matrix.suffix }}
          labels: |
            org.opencontainers.image.title=kageos
            org.opencontainers.image.description=kageos all-in-one local image
            org.opencontainers.image.version=${{ needs.metadata.outputs.version }}
            org.opencontainers.image.source=${{ github.server_url }}/${{ github.repository }}
          build-args: |
            APT_USE_MIRROR=0
            GOPROXY=https://proxy.golang.org,direct
            GOSUMDB=sum.golang.org
            NPM_REGISTRY=https://registry.npmjs.org
            KAGEOS_DEFAULT_APP_BASE_IMAGE=docker.io/qiayanai/kagebase:${{ needs.metadata.outputs.version }}
          cache-from: type=gha,scope=kageos-${{ matrix.suffix }}
          cache-to: type=gha,mode=max,scope=kageos-${{ matrix.suffix }}

  publish:
    name: Publish multi-arch manifest
    needs:
      - metadata
      - build
    runs-on: ubuntu-latest
    timeout-minutes: 190
    steps:
      - name: Log in to Docker Hub
        uses: docker/login-action@v4
        with:
          username: ${{ vars.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Create and verify release manifest
        shell: bash
        env:
          RELEASE_VERSION: ${{ needs.metadata.outputs.version }}
          PUSH_LATEST: ${{ needs.metadata.outputs.push_latest }}
        run: |
          set -euo pipefail

          version_ref="${IMAGE_NAME}:${RELEASE_VERSION}"
          amd64_ref="${version_ref}-amd64"
          arm64_ref="${version_ref}-arm64"

          docker buildx imagetools create --tag "$version_ref" "$amd64_ref" "$arm64_ref"
          if [[ "$PUSH_LATEST" == "true" ]]; then
            docker buildx imagetools create --tag "${IMAGE_NAME}:latest" "$version_ref"
          fi

          manifest="$(docker buildx imagetools inspect "$version_ref")"
          printf '%s\n' "$manifest"
          grep -q 'Platform:.*linux/amd64' <<<"$manifest"
          grep -q 'Platform:.*linux/arm64' <<<"$manifest"

      - name: Sync image to Aliyun ACR
        shell: bash
        env:
          RELEASE_VERSION: ${{ needs.metadata.outputs.version }}
          PUSH_LATEST: ${{ needs.metadata.outputs.push_latest }}
          ALIYUN_REGISTRY: ${{ vars.ALIYUN_REGISTRY }}
          ALIYUN_NAMESPACE: ${{ vars.ALIYUN_NAMESPACE }}
          ALIYUN_REGISTRY_USERNAME: ${{ secrets.ALIYUN_REGISTRY_USERNAME }}
          ALIYUN_REGISTRY_PASSWORD: ${{ secrets.ALIYUN_REGISTRY_PASSWORD }}
        run: |
          set -euo pipefail

          if [[ -z "${ALIYUN_REGISTRY:-}" || -z "${ALIYUN_NAMESPACE:-}" || -z "${ALIYUN_REGISTRY_USERNAME:-}" || -z "${ALIYUN_REGISTRY_PASSWORD:-}" ]]; then
            echo "Aliyun registry is not configured; skipping image sync."
            exit 0
          fi

          if ! command -v skopeo >/dev/null 2>&1; then
            sudo apt-get update
            sudo apt-get install -y skopeo
          fi

          copy_image() {
            local source="$1"
            local target="$2"
            local attempt

            for attempt in 1 2 3; do
              echo "Copying ${source} -> ${target} (attempt ${attempt}/3)"
              if timeout 30m skopeo copy --all "$source" "$target"; then
                skopeo inspect --format 'Target digest: {{.Digest}}' "$target" || true
                return 0
              fi

              echo "Copy failed for ${target}"
              if [[ "$attempt" -lt 3 ]]; then
                sleep $((attempt * 30))
              fi
            done

            return 1
          }

          version="${RELEASE_VERSION}"
          push_latest="${PUSH_LATEST}"
          source_image="docker://${IMAGE_NAME}:${version}"
          target_image="docker://${ALIYUN_REGISTRY}/${ALIYUN_NAMESPACE}/kageos"

          printf '%s' "$ALIYUN_REGISTRY_PASSWORD" | skopeo login --username "$ALIYUN_REGISTRY_USERNAME" --password-stdin "$ALIYUN_REGISTRY"
          copy_image "$source_image" "${target_image}:${version}"
          if [[ "$push_latest" == "true" ]]; then
            copy_image "$source_image" "${target_image}:latest"
          fi

      - name: Publish latest release pointer
        if: needs.metadata.outputs.push_latest == 'true'
        shell: bash
        env:
          RELEASE_VERSION: ${{ needs.metadata.outputs.version }}
          AWS_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}
          R2_ACCOUNT_ID: ${{ secrets.R2_ACCOUNT_ID }}
          R2_BUCKET: ${{ vars.R2_BUCKET }}
        run: |
          set -euo pipefail

          if [[ -z "${R2_ACCOUNT_ID:-}" || -z "${AWS_ACCESS_KEY_ID:-}" || -z "${AWS_SECRET_ACCESS_KEY:-}" ]]; then
            echo "R2 credentials are not configured; skipping latest release pointer upload."
            exit 0
          fi

          if ! command -v aws >/dev/null 2>&1; then
            python3 -m pip install --user awscli
            export PATH="${HOME}/.local/bin:${PATH}"
          fi

          bucket="${R2_BUCKET:-kageos-releases}"
          endpoint="https://${R2_ACCOUNT_ID}.r2.cloudflarestorage.com"
          mkdir -p dist/r2/releases
          printf '%s\n' "$RELEASE_VERSION" > dist/r2/releases/latest.txt
          aws --endpoint-url "$endpoint" s3 cp dist/r2/releases/latest.txt "s3://${bucket}/releases/latest.txt" \
            --cache-control "no-cache, max-age=60"
