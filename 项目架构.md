# AI-Agent-OS 项目架构说明

> 本文基于当前仓库代码与已有说明整理，用于让团队成员、合作伙伴、部署人员和大模型快速理解 AI-Agent-OS 的整体架构、核心链路和模块边界。

---

## 1. 项目定位

AI-Agent-OS 是一个 **AI 原生的轻应用工作台**。

它的核心目标不是做一个普通聊天机器人，也不是只做代码生成工具，而是把企业里的高频业务需求变成可运行、可治理、可复用的标准应用资产。

用户在工作台里用自然语言描述需求后，平台会把需求收敛到以下标准能力中：

- `Form`：一次输入，一次输出，例如文件转换、数据处理、生成报告。
- `Table`：管理一批记录，例如工单、客户、订单、库存、招聘。
- `Chart`：基于已有数据做统计图表，例如趋势、占比、完成率。
- `Docs`：文档、规范、知识沉淀。

平台统一负责：

- 服务树组织
- 动态 UI 渲染
- 权限与组织
- 操作日志
- 文件存储
- 应用运行时
- 版本发布
- 能力包复用
- 工作台 Agent 生成与执行

一句话概括：

> AI-Agent-OS 把“企业新需求”从自然语言变成挂在统一服务树上的标准能力单元。

---

## 2. 总体架构

### 2.1 架构总览

```text
用户 / 浏览器
  |
  v
Web 前端
  - 工作空间
  - 服务树
  - 智能工作台
  - Form / Table / Chart 动态渲染
  |
  v
API Gateway
  - 统一入口
  - 按路径分流
  |
  +--------------------+---------------------+--------------------+
  |                    |                     |                    |
  v                    v                     v                    v
app-server        agent-server          app-runtime          app-storage
应用元数据        工作台 Agent          应用运行时           文件上传下载
服务树            LLM 调用              Podman 容器          MinIO / 存储
权限历史          工具编排              NATS 调用
Docs              Prompt 规范           版本管理
  |
  v
hr-server
用户 / 部门 / 认证

外部生态：
  应用市场后续重写
  - 旧版 Hub 独立服务已移除
  - 当前 MVP 不依赖应用市场链路
```

### 2.2 核心模块

| 模块 | 路径 | 职责 |
|---|---|---|
| Web 前端 | `web/` | 工作空间、服务树、工作台对话、动态组件渲染、文件交互 |
| API Gateway | `core/api-gateway` | 统一 HTTP 入口，按 path 转发到不同后端服务 |
| app-server | `core/app-server` | 应用、服务树、函数元数据、Docs、权限、历史 |
| agent-server | `core/agent-server` | 工作台会话、LLM 调用、Prompt、ToolRegistry、代码生成、工具执行 |
| app-runtime | `core/app-runtime` | 应用容器生命周期、编译发布、版本管理、NATS 调用 SDK App |
| app-storage | `core/app-storage` | 文件上传、下载、去重、存储、访问地址 |
| hr-server | `core/hr-server` | 用户、部门、认证等组织能力 |
| control-service | `core/control-service` | 平台控制类服务能力 |
| backup-service | `core/backup-service` | 备份、恢复、运维数据保护 |
| SDK | `sdk/agent-app` | 业务应用开发协议，提供 Form/Table/Chart、widget、callback、response |

> Hub 已从当前主仓库移除，后续按新方案重写；当前 MVP 不再维护独立 Hub 服务。

---

## 3. 核心抽象

### 3.1 ServiceTree

ServiceTree 是平台的统一资源组织方式。

它类似操作系统目录树，每个节点都有全局路径 `full_code_path`。这个路径贯穿：

- 菜单展示
- API 路由
- 权限控制
- 文件归属
- 工作台上下文
- 能力包复用
- 函数搜索与执行

节点类型主要包括：

| 类型 | 说明 |
|---|---|
| `package` | 目录，用于组织业务模块 |
| `function` | 可执行能力，通常是 Form/Table/Chart |
| `docs` | 文档节点 |

### 3.2 Form / Table / Chart

AI-Agent-OS 用三个核心函数类型承载大多数企业轻应用需求。

| 类型 | 本质 | 典型场景 |
|---|---|---|
| Form | 一次提交，一次返回 | 视频转 MP4、PDF 提取、Excel 解析、生成报告、提交投票 |
| Table | 管理一批记录 | 工单管理、客户管理、会议室预约、库存管理、招聘投递 |
| Chart | 查询并展示统计图 | 工单办结率、销售趋势、分类占比、库存预警 |

这三个抽象的价值在于：

- 前端可以统一渲染。
- 后端可以统一注册。
- Agent 可以统一理解和调用。
- 权限、日志可以统一挂载。
- 能力包可以统一导入导出。

### 3.3 SDK 契约

业务应用通过 `sdk/agent-app` 接入平台。

SDK 主要提供：

- `app.TableTemplate`
- `app.FormTemplate`
- `app.ChartTemplate`
- `widget` 标签协议
- `callback` 回调协议
- `response` 构建器
- 文件输入输出能力
- 当前用户、数据库、上下文能力

业务代码只需要定义：

- 字段结构
- widget 元数据
- 查询逻辑
- 表单处理逻辑
- 图表聚合逻辑
- 必要的业务回调

平台不要求每个业务系统单独写前端页面。

---

## 4. 工作台 Agent 架构

### 4.1 工作台定位

工作台是 AI-Agent-OS 的核心入口。

它不是普通对话框，而是一个绑定当前目录上下文的智能操作台，能完成：

- 解释当前项目
- 创建新系统
- 修改已有系统
- 查表格数据
- 提交表单
- 查询图表
- 调用已有工具
- 导入或导出能力包

### 4.2 工作模式

| 模式 | 目标 | 典型动作 |
|---|---|---|
| 开发模式 | 创建新应用或新功能 | 读文档、读案例、出 PRD、写代码、编译 |
| 修改模式 | 修改已有功能 | 读现有代码、给修改方案、局部改代码、编译 |
| 执行模式 | 操作已有应用 | 查表、提交表单、查图表、新增/更新记录 |
| 杂活任务 | 调用通用工具 | 搜工具、填参数、执行、返回文件 |

### 4.3 ToolRegistry

`agent-server` 内置 ToolRegistry，统一注册工作台可调用工具。

工具大致分三类：

| 类型 | 工具示例 | 说明 |
|---|---|---|
| 工作区工具 | `read_go_file`、`write_go_file`、`search_replace_file`、`build_workspace` | 读写和编译当前应用 |
| 运行时工具 | `run_form_submit`、`run_table_search`、`run_table_create`、`run_table_update`、`run_chart_query` | 执行已注册 Form/Table/Chart |
| 平台工具 | `search_tools`、`copy_directory`、`record_workspace_event` | 搜索工具、目录复用和事件记录 |

### 4.4 创建应用链路

```text
用户描述需求
  |
  v
工作台判断能力边界
  - 是否属于平台侧能力
  - 是否能映射到 Form / Table / Chart
  - 是否有可用工具或库
  - 是否在请求-响应范围内
  |
  v
读取 SDK 文档和匹配案例
  |
  v
输出 PRD
  - 字段
  - 列表
  - 业务规则
  - 是否新建目录
  |
  v
用户确认
  |
  v
写 Go 代码
  |
  v
build_workspace 编译并部署
  |
  v
调用 run_* 工具验证
  |
  v
可用后返回结果
```

关键原则：

- 禁止未读文档就生成方案。
- 禁止未确认 PRD 就写代码。
- 禁止伪代码和占位实现。
- 改完必须编译。
- 有可执行函数时必须尽量验证。

### 4.5 执行与杂活链路

当用户说：

- “帮我把这个视频转成 MP4”
- “帮我读取 PDF 内容”
- “帮我把 Excel 画成折线图”
- “查一下所有待处理工单”
- “新增一条客户记录”

工作台优先走结构化执行：

```text
用户提出任务
  |
  v
先看当前目录是否已有可执行函数
  |
  +-- 有 -> 直接 run_form_submit / run_table_search / run_chart_query
  |
  +-- 没有
        |
        v
     search_tools 搜系统内已注册工具
        |
        +-- 搜到 -> 按 request schema 填参数并执行
        |
        +-- 搜不到 -> 进入创建流程
```

这条链路的优势是：

- 不让 Agent 临场猜命令。
- 不让 Agent 临场拼脚本。
- 工具有明确 schema。
- 文件输出链路稳定。
- 好用的工具可以继续沉淀为平台资产。

---

## 5. 应用运行时架构

### 5.1 Runtime 职责

`app-runtime` 负责应用生命周期和运行时调用。

主要能力：

- 创建应用
- 更新应用
- 编译应用
- 启动新版本
- 维护版本元数据
- 应用发现
- NATS 调用 SDK App
- 优雅关闭旧版本
- 清理无流量旧版本
- 工作区源码文件读写与回滚

### 5.2 版本与发布

应用版本元数据位于 `workplace/metadata`：

| 文件 | 作用 |
|---|---|
| `current_version.txt` | 当前生效版本 |
| `current_app.txt` | 当前应用二进制名前缀 |
| `version.json` | 版本索引，记录当前版本、最新版本和历史版本状态 |

发布主流程：

```text
工作台写入源码
  |
  v
build_workspace
  |
  v
app-runtime 编译新版本到 workplace/bin/releases
  |
  v
更新 workplace/metadata
  |
  v
启动新版本容器
  |
  v
等待启动确认和 update callback
  |
  v
更新数据库版本记录
  |
  v
清理无流量旧版本
```

### 5.3 NATS 调用模型

平台服务之间通过 NATS 做 request-reply 和事件通信。

典型调用链：

```text
Web 前端点击 Form/Table/Chart
  |
  v
API Gateway
  |
  v
app-server
  |
  v
app-runtime
  |
  v
NATS request
  |
  v
SDK App 容器内函数
  |
  v
返回结构化响应
  |
  v
前端动态渲染结果
```

---

## 6. 前端架构

Web 前端位于 `web/`，主要技术栈：

- Vue 3
- TypeScript
- Element Plus
- Pinia
- Vue Router
- ECharts
- Monaco Editor
- TipTap / Vditor 等编辑能力

主要职责：

- 登录与工作空间入口
- 服务树展示与操作
- 工作台会话流式展示
- 文件上传与预览
- Form 动态渲染
- Table 动态渲染
- Chart 动态渲染
- Docs 编辑展示
- 能力包导入导出交互

前端不为每个业务应用写独立页面，而是根据后端注册的 schema 和 widget 元数据统一渲染。

---

## 7. 文件与存储架构

`app-storage` 负责统一文件能力。

典型场景：

- 用户在工作台上传附件。
- Form 字段上传文件。
- 视频、PDF、图片、Excel 等工具处理输入文件。
- 工具输出文件返回给用户下载或预览。

文件处理推荐模式：

```text
上传文件
  |
  v
获得 bucket/object_key 引用
  |
  v
run_form_submit 传入文件引用
  |
  v
SDK App 下载输入文件到临时目录
  |
  v
业务逻辑处理
  |
  v
输出写入 trace output dir
  |
  v
ResponseFiles 返回可访问附件
```

这让文件处理工具可以稳定沉淀为标准 Form。

---

## 8. 权限、审批与组织

AI-Agent-OS 的治理能力不是每个应用自己实现，而是平台横切提供。

主要能力：

| 能力 | 说明 |
|---|---|
| 权限管理 | 基于 `full_code_path` 做资源权限控制，目录权限可继承 |
| 组织管理 | 用户、部门、组织结构由 hr-server 提供 |
| 操作日志 | 平台统一记录关键操作 |

业务代码不应该重复实现：

- 权限判断
- 操作日志表
- 评论系统
- 通用 UI 样式

---

## 9. 应用市场

旧版 Hub 独立服务已从当前主仓库移除，后续按新方案重写。当前 MVP 阶段不再维护独立 Hub 服务，也不把应用市场作为主链路依赖。

后续重写时再重新定义：

- 好用的目录如何发布出去。
- 其他用户如何复制到自己的工作空间。
- 目录如何版本化更新。
- 工具、模板、行业方案如何复用和服务化。

旧 Hub 的具体路由、数据库、前后端实现和部署资源不再作为当前架构的一部分。

---

## 10. 部署架构

当前官方生产入口位于：

```text
deploy/prod/
```

快速部署核心命令：

```bash
go run ./cmd/aosctl init --base-url http://your-ip-or-domain
go run ./cmd/aosctl up --config deploy/prod/aos.yaml
```

生产单机部署包含：

- 主站
- MySQL
- NATS
- MinIO
- 内置 Nginx
- 容器内 Podman

逻辑结构：

```text
公网 :80 / :443
  |
  v
main 容器（host network）
  |
  +-- Nginx
  |     - Web 静态文件
  |     - SPA fallback
  |     - API Gateway 反向代理
  |
  +-- API Gateway
  |
  +-- core-server
        - app-server
        - agent-server
        - app-runtime
        - hr-server
        - app-storage
        - control-service

中间件
  - MySQL
  - NATS
  - MinIO
```

用户应用不是直接跑在平台进程里，而是由 runtime 通过 Podman 启动独立应用容器。

---

## 11. 关键业务链路

### 11.1 生成“百姓呼声工单系统”

```text
用户：
  需要一个百姓呼声工单管理系统，支持分类、部门流转、办理时限、满意度统计

工作台：
  1. 判断这是 Table + Chart 场景
  2. 读取能力边界、SDK、工单案例
  3. 输出 PRD
  4. 用户确认
  5. 创建目录和 Go 文件
  6. build_workspace
  7. 调用 run_table_search / run_chart_query 验证

结果：
  生成工单列表、筛选、状态、部门、办理时限、满意度字段与统计图表
```

### 11.2 视频转 MP4

```text
用户：
  帮我把这个视频转换成 MP4

工作台：
  1. 判断这是单次文件处理，属于 Form 工具
  2. 当前目录无工具时 search_tools("视频|ffmpeg|mp4", "form")
  3. 找到视频转换工具
  4. 按 schema 填 input_files、output_format
  5. run_form_submit 执行

结果：
  返回 output.mp4 文件
```

### 11.3 应用市场复用（后续重写）

```text
用户：
  我需要一个招聘投递系统

工作台：
  1. 先搜索当前目录能力
  2. 应用市场能力后续按新方案重写
  3. 当前 MVP 优先在工作空间内生成与复用
  4. copy_directory 仅处理当前主链路支持的目录复制
  5. 用户按需修改字段或规则

结果：
  当前阶段先保证从零生成与本地目录复用稳定
```

---

## 12. 架构边界

AI-Agent-OS 适合：

- 企业内部管理系统
- 轻量业务工具
- 文件处理工具
- 数据处理工具
- 统计图表
- 表单收集
- 审批流转
- 目录化知识与讨论
- 可复用模板与工具市场

当前不适合：

- 高度自定义前端交互
- 实时双向通信应用
- 在线 IDE
- 视频剪辑器
- 直播系统
- 游戏
- 复杂 WebGL / 3D 可视化
- 需要长期持续运行且无法拆成单次任务的后台服务

判断原则：

```text
能否映射到 Form / Table / Chart？
  |
  +-- 不能：大概率不是当前平台目标
  |
  +-- 能：
        |
        v
是否有成熟工具或库支撑？
  |
  +-- 没有：给降级方案
  |
  +-- 有：
        |
        v
是否能在一次请求-响应内完成？
  |
  +-- 不能：不适合
  |
  +-- 能：可以做
```

---

## 13. 架构优势

### 13.1 和普通 Agent 的区别

普通 Agent 更像是在临场想办法干活。

AI-Agent-OS 的工作台则优先搜索和调用标准能力：

- 有 schema
- 有权限
- 有文件链路
- 有执行记录
- 有前端渲染
- 有能力包复用

### 13.2 和传统低代码的区别

传统低代码通常强调拖拽和配置。

AI-Agent-OS 强调：

- 用户说业务目标。
- Agent 产出 PRD 和代码。
- 平台统一运行和治理。
- 生成能力可以继续被搜索、调用、复制和发布。

### 13.3 和普通代码生成的区别

普通代码生成的产物往往只是代码。

AI-Agent-OS 的产物会自动进入：

- 服务树
- 运行时
- 动态 UI
- 权限治理
- 操作日志
- 文件系统
- 工具搜索
- 能力包复用

---

## 14. 推荐对外表述

可以用下面这段作为项目架构的简短介绍：

> AI-Agent-OS 是面向个人用户和小团队的 AI 轻应用工作台。它用 ServiceTree 统一组织资源，用 Form/Table/Chart 承载高频业务，用工作台 Agent 完成 PRD、代码生成、编译部署和执行验证，并用 app-runtime 管理容器化应用版本。
