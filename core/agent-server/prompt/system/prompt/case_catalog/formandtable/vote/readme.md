# 投票系统案例

本目录收录投票系统案例。`prd.json` 提供 PRD v2 结构化样例，`prd.md` 提供实现参考和代码说明，适合多模块协同、投票规则和统计结果场景。

本案例同时演示富内容下拉：主题只保留一个富文本“主题说明”，回调通过 `SelectFuzzyItem.RichText` 展示；选项回调通过 `Files` 展示候选图片。富文本已能插入图片和附件时，不再重复设计普通描述和独立主题附件字段；`Value` 始终只保存并提交主题 ID 或选项 ID，短属性和聚合数据继续使用 `DisplayInfo` / `Statistics`。
