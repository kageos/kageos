# Form 串联工作流案例

本案例演示最小可运行的工作流：`workflow.start` 声明上传文件和摘要风格，随后调用第一个 Form 提取文本，再把输出映射到第二个 Form 生成摘要，最后由 `workflow.output` 声明最终响应字段。

适合用户要求“多个 Form 串起来”“上一步输出作为下一步输入”“先做工作流 MVP”的场景。案例中的函数路径和字段 code 是示例，真实项目必须以 `search_tools` 返回结果为准。
