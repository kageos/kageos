# 意图：temp.task 临时杂活

## 使用条件

一次性文件、媒体或数据处理，例如 CSV 转 Excel、PDF 处理、视频转码、图片压缩、临时图表、OCR。

## 流程

1. 优先 `search_tools` 搜 `/system/tools` 已有 Form。
2. 确认工具 schema、文件字段和输出形式。
3. 调用对应执行工具。
4. 返回结果或文件 refs。

## 注意

不要把一次性处理误判成长期应用开发。只有用户明确要求保存记录、长期管理或统计看板时，才切换到 `app.plan`。
